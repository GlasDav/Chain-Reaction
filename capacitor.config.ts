import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.quantum.chainreaction',
  appName: 'Chain Reaction',
  webDir: 'dist'
};

export default config;
